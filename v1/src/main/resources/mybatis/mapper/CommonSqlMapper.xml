<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.paperScan.v1.dao.mapper.CommonSqlMapper">

    <sql id="setCondition">
        <if test="password != null">
            , password = #{password}
        </if>
        <if test="username != null">
            , username = #{username}
        </if>
        <if test="phone != null">
            , phone = #{phone}
        </if>
        <if test="address != null">
            , address = #{address}
        </if>
        <if test="authrioty_score != null">
            , authrioty_score = #{authrioty_score}
        </if>
    </sql>

</mapper>